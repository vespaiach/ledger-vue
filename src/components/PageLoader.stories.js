import PageLoader from './PageLoader.vue';

export default {
  title: 'PageLoader',
  component: PageLoader,
};

export const Default = () => ({
  components: { PageLoader },
  template: '<PageLoader />',
});